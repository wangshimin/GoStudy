
	// //	获取对象的字段、类型、值
	// for i := 0; i < t.NumField(); i++ {
	// 	f := t.Field(i)
	// 	val := v.Field(i).Interface()
	// 	fmt.Printf("%6s: %v = %v \n", f.Name, f.Type, val)
	// }

	// // 获取对象的方法名、方法签名
	// for i := 0; i < t.NumMethod(); i++ {
	// 	m := t.Method(i)
	// 	fmt.Printf("%6s: %v \n", m.Name, m.Type)
	// }